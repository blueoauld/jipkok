api.jipkok.app {
    tls /certs/origin.pem /certs/origin-key.pem

    reverse_proxy server:8080 {
        lb_try_duration 30s
        lb_try_interval 250ms
    }

    encode zstd gzip

    log {
        output stdout
        format json
    }
}
